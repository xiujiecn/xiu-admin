# 项目约束规范
## xiuAdmin框架说明文档
    先阅读 http://xiuadmin.com/sys/catalog.html
## 范围界定
1. 本仓库包含两部分代码：
   - 后端：基于 GoFrame v2 框架，存放于 `server/` 目录
   - 前端：Vue + Vben Admin 技术栈，存放于 `vue/` 目录
2. GoFrame 项目专属规范仅适用于 `server/` 下后端开发，或用户明确要求进行 GoFrame 开发的场景。
3. 仅修改前端代码时，不得套用 GoFrame 后端开发规范。
4. `vue/` 目录下前端开发需遵循 Vben Admin 5.x 开发规范，同步沿用 `vue/apps/web-antd` 中已落地的 Ant Design Vue 页面风格。

## GoFrame 后端开发规范
1. 控制器、服务、中间件、路由、配置、ORM、自动生成代码均严格遵循 GoFrame v2 官方编码惯例。
2. 禁止手动编辑自动生成的 DAO、DO、Entity 文件，统一使用项目内置代码生成流程更新。
3. 新增逻辑前优先复用项目现有服务、工具函数与通用设计模式，避免重复新增抽象层。
4. 数据库新增、更新操作必须使用自动生成的 DAO/DO 结构体；禁止将 `g.Map`、`map[string]interface{}` 作为 ORM 数据载体。
5. 后端新增业务报错统一使用 `gerror` 组件封装并返回错误信息。
6. 常规 ORM 操作交由 GoFrame 自动维护 `created_at`、`updated_at`、`deleted_at` 字段；逻辑删除统一调用 `Delete()` 方法。

## 运行环境说明
1. 后端配置文件路径：`server/config/config.yaml`
2. 前端主应用目录：`vue/apps/web-antd`
3. 运行日志统一输出至项目根目录 `log/` 文件夹；Git 仅保留 `log/.gitkeep` 文件，日志文件禁止提交至代码仓库。
4. 项目二次开发版本号采用三段式格式，初始版本为 `v0.0.1`。
5. 默认用户名密码是 admin，密码是 123456。

## Vben 前端开发规范
1. `vue/` 采用 pnpm 多包仓库架构；新增功能前优先复用内置工具包：`@vben/common-ui`、`@vben/hooks`、`@vben/request`、`@vben/stores`、`@vben/preferences`、`@vben/utils`，以及应用内 `#/api` 接口工具，尽量不新增第三方依赖。
2. 前端主应用 `vue/apps/web-antd` 技术栈：Vue3 + Vite + TypeScript + Vben Admin + Ant Design Vue。
3. 代码分层固定规范：
   - 接口请求文件：`vue/apps/web-antd/src/api`
   - 页面视图：`src/views`
   - 路由配置：`src/router/routes`
   - 本地状态管理：`src/store`
   - 全局偏好配置：`src/preferences.ts`
4. 沿用 `src/preferences.ts` 中已配置的后端交互模式；菜单、路由、按钮权限控制需适配后端动态权限体系，统一使用项目现有 `v-access` 权限指令。
5. 工作台/数据大盘类页面，优先使用 Vben 内置封装组件：`WorkbenchHeader`、`WorkbenchProject`、`WorkbenchQuickNav`、`WorkbenchTodo`、`WorkbenchTrends`，以及 `@vben/common-ui` 提供的图表卡片，不自行从零搭建卡片布局。
6. 管理类页面风格要求：信息紧凑、易快速浏览、侧重业务操作；优先使用表格、筛选器、抽屉/弹窗、工具栏操作、状态标签、简洁导航，不使用营销风大横幅模块。
7. 图标统一沿用项目已引入的 Iconify 图标命名规范；库中已有图标时，禁止自定义绘制图标。
8. 接口请求逻辑严格遵循 `vue/apps/web-antd/src/api/request.ts` 现有封装，包含 Token 处理、登录过期拦截、统一返回体解析逻辑。
9. 修改前端可视化逻辑后，需在本地开发地址 `http://localhost:5888/` 运行 Vite 服务验证效果。

## Vben Ant 工作台标准页面布局规范
1. 登录后后台统一布局框架：左侧侧边栏导航、顶部操作栏、面包屑、标签栏、可滚动内容区。
2. 侧边栏菜单分组展示，搭配图标；当前激活菜单项使用浅蓝高亮，侧边栏收起/展开控件常驻显示。
3. 顶部操作栏简洁实用，功能包含：侧边栏切换、页面刷新、全局搜索、系统设置、暗黑模式、语言切换、全屏、消息通知、用户头像。
4. 标签栏展示所有已打开页面并支持关闭，路由跳转需保证页面标题清晰可读。
5. 工作台内容区规范：页面主体预留内边距；首行放置欢迎语+数据统计面板；下方采用响应式双栏布局：左侧为主业务卡片，右侧放置快捷导航、待办事项、图表模块。
6. 卡片样式统一：细边框、小圆角、简洁标题，列表/网格内容以快速浏览为设计目标。
7. 新增业务页面需贴合该后台框架风格：禁止独立落地页、超大横幅、与后台框架冲突的装饰性布局。
## 数据库变动
1. 数据库任何变动必须生成变动记录放到 docs/db目录下，注明变动日期时间、变动原因、变动内容和SQL语句。

## 系统菜单权限
1. 登录系统界面后，左侧菜单栏菜单都是动态路由菜单。菜单数据都放在 sys_menu表。菜单栏里有权限标识（不带前缀）。权限标识有相关的前缀。
    "cpr:"             // 角色权限码前缀+角色权限
    "cpm:"             // 菜单权限码前缀+菜单权限
    "cpd:"             // 部门权限码前缀+部门编码
    "cpp:"             // 岗位权限码前缀+岗位编码
    "cpu:"             // 用户权限码前缀+用户名
    "cpc:current:user" // 当前登录用户默认权限
2. 如果登录的是超级管理员账号 admin，默认返回所有权限。
3. 如果登录的不是超级管理员账号：
    1. 登录用户取得用户的所有角色；
    2. 根据角色取得所有角色绑定的菜单；
    3. 获取菜单中的权限标识。


## 系统测试
```
管理员账号： admin 密码：123456
登录时有图片验证码，如果图片验证码不识别，多点几次，图片验证码自己识别一下。

```
